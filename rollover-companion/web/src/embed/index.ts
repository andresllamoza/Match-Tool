/**
 * Embeddable mount API for PensionBee engineering.
 *
 * Usage (React):
 *   import { EmbedWidget } from '@pensionbee/rollover-companion/embed';
 *   <EmbedWidget theme="minimal" />
 *
 * Usage (iframe):
 *   <iframe src="https://host/embed" ... />
 */
export { EmbedWidget } from "../components/EmbedWidget";
