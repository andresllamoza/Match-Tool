{
  "name": "match-tool",
  "private": true,
  "description": "Monorepo root. Vercel Root Directory must be set to: web",
  "dependencies": {
    "next": "14.2.35"
  },
  "scripts": {
    "vercel-build": "npm run build --prefix web"
  }
}
