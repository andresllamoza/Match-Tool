{
  "name": "match-tool",
  "private": true,
  "description": "Monorepo root — customer Next.js app is in rollover-companion/web",
  "scripts": {
    "build": "npm run build --prefix rollover-companion/web",
    "vercel-build": "npm run build --prefix rollover-companion/web"
  }
}
