# Copy to .streamlit/secrets.toml for local development.
# On Streamlit Community Cloud, set these in the app's Secrets UI — do not commit real values.

app_password = "REPLACE_ME_WITH_A_REAL_PASSWORD"
