# TEMPLATE — never commit the real secrets.toml to git.
#
# LOCAL DEV:
#   1. Copy this file to .streamlit/secrets.toml (drop the .example suffix)
#   2. Replace the placeholder with a real password
#
# STREAMLIT CLOUD DEPLOYMENT:
#   1. Do NOT add secrets.toml to git (already in .gitignore)
#   2. On share.streamlit.io, open your app → Settings → Secrets
#   3. Paste the contents of your local secrets.toml there
#   4. Save and the app reloads

app_password = "REPLACE_ME_WITH_A_REAL_PASSWORD"
